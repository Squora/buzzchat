# BuzzChat

Корпоративный мессенджер с групповыми чатами, личными сообщениями и WebSocket-соединениями в реальном времени.

## 🎯 Описание

BuzzChat - современное приложение для корпоративных коммуникаций, построенное на микросервисной архитектуре с разделением ответственности между REST API, WebSocket gateway и фронтенд-приложением.

## 🛠 Технологии

### Backend API
- **PHP 8.2+** + **Symfony 7.1**
- **PostgreSQL 16** + **Doctrine ORM**
- **JWT Authentication** (Access + Refresh tokens)
- **API Platform 4** для REST API
- Clean Architecture + CQRS

### Frontend
- **React 19** + **TypeScript 5.9**
- **Vite 7** для сборки
- **Sass** для стилизации
- **Lucide React** для иконок

### WebSocket Gateway
- **Go 1.24** + **gorilla/websocket**
- Управление WebSocket-соединениями

## 📦 Структура проекта

```
buzzchat/
├── backend-api/          # Symfony REST API
├── frontend/             # React SPA
├── gogate/               # Go WebSocket Gateway
├── docker-compose.yml    # Запуск всех сервисов
└── README.md            # Этот файл
```

## 🚀 Быстрый старт

### Требования
- Docker + Docker Compose
- (Опционально) PHP 8.2+, Node.js 20+, Go 1.24+ для локальной разработки

### Запуск через Docker Compose

```bash
# Клонировать репозиторий
git clone <repo-url>
cd buzzchat

# Запустить все сервисы
docker compose up -d

# Backend API будет доступен на http://localhost:8080
# Frontend будет доступен на http://localhost:5173
# GoGate будет доступен на http://localhost:8081
```

### Ручной запуск (для разработки)

#### Backend API
```bash
cd backend-api

# Установить зависимости
composer install

# Запустить PostgreSQL
docker compose up -d database

# Выполнить миграции
php bin/console doctrine:migrations:migrate

# Сгенерировать JWT ключи
php bin/console lexik:jwt:generate-keypair

# Запустить сервер
symfony server:start
# или
php -S localhost:8000 -t public/
```

#### Frontend
```bash
cd frontend

# Установить зависимости
npm install

# Запустить dev сервер
npm run dev
```

#### GoGate
```bash
cd gogate

# Установить зависимости
go mod download

# Запустить сервер
go run main.go
```

## 📚 Функциональность

### Аутентификация
- Регистрация по телефону с SMS-верификацией
- JWT-токены с refresh token механизмом
- Защищенные API endpoints

### Чаты
- Групповые чаты с ролями (owner, admin, member)
- Личные чаты (direct messages)
- Управление участниками
- Поиск и фильтрация
- Оптимизация для больших чатов (500+ участников)

### Пользователи
- Профили с фото, должностью, статусом
- Отделы (departments)
- Онлайн-статусы (available, busy, away, offline)
- Настройки (тема, уведомления, язык)

## 📖 Документация

Подробная документация находится в каждом модуле:
- [Backend API](./backend-api/README.md) - API документация, архитектура
- [Frontend](./frontend/README.md) - Компоненты, структура
- [GoGate](./gogate/README.md) - WebSocket протокол

## 🧪 Тестирование

### Backend
```bash
cd backend-api
./vendor/bin/phpunit
```

## 🔧 Конфигурация

### Переменные окружения

Скопируйте `.env` файлы в каждом модуле и настройте под свои нужды:

**backend-api/.env**
```env
DATABASE_URL=postgresql://buzzchat:buzzchat@localhost:5432/buzzchat
JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem
JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem
JWT_PASSPHRASE=your-secret-passphrase
```

## 📝 Лицензия

MIT

## 👤 Автор

Создано как pet-проект для демонстрации навыков full-stack разработки.
